# tools/

Python build and asset-pipeline scripts: converting original `.gob`
sprites, PCX levels, and `.mod`/`.smp` audio into Godot-native PNG/OGG/WAV
resources (`TASK-013`), plus boundary/purity validators wired into
`task check` (e.g. `validate_abi_test_purity.py`, following
`~/git/neo_snake/tools/`).

Placeholder until `TASK-013` and related tasks add scripts here.
